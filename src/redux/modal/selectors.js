export const selectStateModal = state => state.modal.isOpen;

export const selectTypeModal = state => state.modal.modalType;

export const selectModalId = state => state.modal.modalId;

export const selectModalInfo = state => state.modal.modalInfo